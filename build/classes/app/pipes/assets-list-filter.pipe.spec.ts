import { AssetsListFilterPipe } from './assets-list-filter.pipe';

describe('AssetsListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AssetsListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
